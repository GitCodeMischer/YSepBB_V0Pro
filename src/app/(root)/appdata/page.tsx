import AppData from "@/components/appdata/appdata";
import contactMetadata from "@/metadata/contact";

export const metadata = contactMetadata;

export default function AppDataEntry() {
  return (
    <AppData />
  );
}
